// const rateLimit = require("express-rate-limit");

// const limiter = rateLimit({
//   windowMs: 1 * 60 * 1   , // 15 minutes
//   max: 100000 // limit each IP to 100 requests per windowMs
// });

// //  apply to all requests
// module.exports = limiter;